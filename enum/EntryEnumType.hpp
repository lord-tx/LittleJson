#ifndef ENTRYENUMTYPE_HPP_INCLUDED
#define ENTRYENUMTYPE_HPP_INCLUDED

// This enum describes the Entry states for
// the current parsing pointer

enum EntryEnumType{
    KEY,
    VALUE,
    NONE,
};


#endif // ENTRYENUMTYPE_HPP_INCLUDED